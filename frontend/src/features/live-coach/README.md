# live-coach

Live coach beta feature.

Current modules:
- `components/LiveCoachPreviewCard.tsx`: triggers `/live-coach/process` and shows pipeline output (question type, suggestion, structure, transcription metadata).
